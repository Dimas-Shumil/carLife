NODE_ENV=development
PORT=3000
SITE_ORIGIN=http://127.0.0.1:3000
DATABASE_URL="file:./dev.db"

# SMTP is optional. Leads remain in the database if email is unavailable.
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
TO_EMAIL=
